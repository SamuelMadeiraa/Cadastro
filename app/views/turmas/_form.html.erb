<section>
  <div class="container">
    <div class="form-container">
      <%= form_with(model: @turma, local: true, class: "form") do |form| %>
        <% if @turma.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@turma.errors.count, "error") %> prohibited this professor from being saved:</h2>
  
            <ul>
              <% @turma.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
  
        <div class="field">
          <%= form.label :turma %>
          <%= form.text_field :turma , class: "input" %>
        </div>
  
     
  
        <div class="actions">
          <%= form.submit 'Criar aluno'%>
        </div>
      <% end %>
    </div>
  </div>
</section>
  

  