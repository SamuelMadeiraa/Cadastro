<div class="max-w-sm rounded overflow-hidden shadow-lg">
<div class="px-6 py-4">
<%= form_with(model: @turma, local: true) do |form| %>
  <% if @turma.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@turma.errors.count, "error") %> prohibited this professor from being saved:</h2>

      <ul>
        <% @turma.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :codigo_turma %>
    <%= form.text_field :codigo_turma %>
  </div>



  <div class="actions">
    <%= form.submit "Criar turma" %>
  </div>
<% end %>
