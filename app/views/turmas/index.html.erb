<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

  <title>Document</title>
</head>
<body>
  <h1 class="titulo-alunos"><strong>Lista de Turmas cadastrados</strong></h1>
  <table class="table">
    <thead>
      <tr>
        <th>Turmas</th>
      </tr>
    </thead>
    <tbody>
      <% Turma.all.each do |turma| %>
        <tr>
          <td>
            <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#turma<%= turma.id %>">
              Turma <%= turma.turma %>
            </button>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <% Turma.all.each do |turma| %>
    <div class="modal fade" id="turma<%= turma.id %>" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLabel">Turma <%= turma.turma %></h5>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
          </div>
          <div class="modal-body">
            <% Aluno.where(turma_id: turma.id).each do |aluno| %>
              <p><%= aluno.nome_completo %></p>
            <% end %>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fechar</button>
          </div>
        </div>
      </div>
    </div>
  <% end %>

  <button class="botao-aluno">
    <%= link_to 'Nova Turma', new_turma_path %> <%= link_to 'Voltar', turmas_path %>
  </button>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>

</body>
</html>
