<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  
</head>
<body>
  <div class="container">
    <div class="form-container">
      <%= form_with(model: @disciplina, local: true, class: "form") do |form| %>
        <% if @disciplinas.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@disciplina.errors.count, "error") %> prohibited this professor from being saved:</h2>
  
            <ul>
              <% @disciplina.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
  
        <div class="field">
          <%= form.label :materia %>
          <%= form.text_area :materia, class: "input" %>
        </div>
  
        <div class="actions">
          <%= form.submit 'Criar disciplina'%>
        </div>
      <% end %>
    </div>
  </div>
  
</body>
</html>
